# Test Vectors

This directory stores fixture repositories and before/after refactor cases used by
determinism, indexing, and rebinding tests.

Conventions:
- Keep fixtures minimal and focused on one behavior.
- Prefer stable names and deterministic file ordering.
- Avoid machine-specific paths and timestamps in expected outputs.
