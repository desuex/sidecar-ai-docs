# ts-minimal

A tiny TypeScript fixture repository used for deterministic indexing and
reference tests.

Layout:
- `src/math.ts`: exports a function and a type.
- `src/app.ts`: imports and uses the exported symbol.
